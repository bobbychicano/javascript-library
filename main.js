let books = [];

function Book(title, author, pages, read){
  this.title = title;
  this.author = author;
  this.pages = pages;
  this.read = read;
}

function saveInput() {
  // save user input AS AN OBJECT, probably from a form, and add it into the book array
  // will need array methods

  books.push(object);
}
